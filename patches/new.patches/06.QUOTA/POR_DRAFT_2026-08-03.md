# POR — 06.QUOTA room clearing (2026-08-03)

Per `patches/SOP.room-clearing-and-poison-audit.md`. Method: every claim verified against
the LIVE tree (`/home/gorilla/firefox-main`) and the vanilla vault
(`/home/gorilla/Documents/FIREFOX.WORK/Firefox.Scripts.Vault.Docs/SafetyVault.Firefox/firefox-main`),
never against the docs.

Room inventory (2 files): `dom_quota_ActorsParent.cpp.patch`, `MASTER_PROJECT_LOG_06_QUOTA.md`
(consolidates AUDIT + DEVELOPER + LAYMAN + PRECHECK.json + PRECHECK.md, all merged verbatim).
Single target file: `dom/quota/ActorsParent.cpp`. Three changes in one patch.

## Friendlies (kept — verified real)

| Claim | Ground truth | Evidence |
|---|---|---|
| The 1 surviving .patch is a true record | **vanilla + `patch -p1` == live, byte-exact** | staged vault copy → `patch -p1` (clean, no fuzz) → `cmp` == "BYTE-EXACT MATCH" |
| `kAboutHomeOriginPrefix = "moz-safe-about:home"` added | present, exact value | ActorsParent.cpp:268 |
| about:home whitelisted in the first-prompt bypass alongside chrome:// and resource:// | present | ActorsParent.cpp:7779 (`StringBeginsWith(aOrigin, nsDependentCString(kAboutHomeOriginPrefix))`) inside `QuotaManager::IsOriginInternal` (7774) |
| console-diagnostic gate swapped `NIGHTLY_BUILD` → `EARLY_BETA_OR_EARLIER` | present, correct gate | ActorsParent.cpp:4297 wraps the negative-usage `console->LogStringMessage(...)` block (4298–4312) — confirmed it is the console diagnostic, not one of the 6 other unrelated `#ifdef NIGHTLY_BUILD` gates (3120/3153/4095/4191/4241/4368) |
| private-browsing origin-map clear removed from temporary-storage shutdown | removed (no `.Clear()` on either map anywhere) | byte-exact patch; `grep mOriginToStorageOriginMap / mStorageOriginToOriginMap` → only insert/lookup sites remain (8699 `TryLookupOrInsertWith`, 8724 `WithEntryHandle`, 8738 `MaybeGet`) |
| "those maps are managed elsewhere" (DEVELOPER doc) | TRUE — they are still populated/queried | ActorsParent.cpp:8699, 8724, 8738 |
| PRECHECK: patch sha256(16)=`2814f13dbeb54536`, 42 lines | both match | `sha256sum` → `2814f13dbeb545364ba9…`; `wc -l` → 42 |
| AUDIT "VERIFICATION COMMANDS" (grep the two symbols) | both pass on live tree | `kAboutHomeOriginPrefix` → 268/7779; `EARLY_BETA_OR_EARLIER` → 4297 |

## Tango neutralised (1 — documentation)

**Fabricated function name in the DEVELOPER track.** `MASTER_PROJECT_LOG_06_QUOTA.md`
line 132 ("Implementation Flow") states the whitelist bypass lives in a predicate named
**`IsFirstOriginQuotaPromptRequired`**. That symbol **does not exist** anywhere:
`grep -rn 'IsFirstOriginQuotaPromptRequired' dom/quota/` → NOT FOUND. The real predicate is
**`QuotaManager::IsOriginInternal`** (defined ActorsParent.cpp:7774, declared QuotaManager.h:744,
called from OriginOperations.cpp:1681 & :3914 and ActorsParent.cpp:9587). The name appears to
have been invented from the in-code comment `// The first prompt is not required for these
origins.` (ActorsParent.cpp:7778). Falsifiable check that caught it: the two whole-tree greps
above. **Impact:** a developer chasing the change by the documented name finds nothing; the CODE
is correct and the patch is faithful — this is a doc-only inaccuracy, not a code defect.
**Action:** correction owed in the master log (whitelist predicate = `IsOriginInternal`, not
`IsFirstOriginQuotaPromptRequired`). Not corrected here — this POR is append-only and does not
edit the master log.

## Housekeeping flags (not poison)

- **No `// GORILLA OVERRIDE:` provenance markers on any of the three changes**, though
  CLAUDE.md mandates them for our edits to Mozilla source (`grep GORILLA ActorsParent.cpp`
  → none). Two readings, and I cannot resolve which from the tree alone:
  (a) these are Gorilla edits with the required markers missing → provenance owed; or
  (b) they are **upstream mozilla-central changes** captured as drift between the vanilla
  snapshot (vault mtime Jul 10) and the live pull (Jul 13), not project customizations.
  Reading (b) is plausible: none of the three are resource optimizations (the project's
  signature — see resource-rule-of-thumb), they read as ordinary upstream correctness fixes,
  and the two trees are only ~3 days apart. The master log itself does NOT claim Gorilla
  authorship or resource savings, so nothing is over-claimed — but the human should decide
  whether 06.QUOTA is documenting project work or upstream drift.
- **"the clear was redundant" (DEVELOPER doc) is partly unsupported.** The maps are used
  elsewhere (true), but after the patch **no `.Clear()` exists on either map anywhere** — so
  "redundant" cannot mean "another clear does it." Read charitably as "unnecessary / a
  stale-mutex-hold hazard" (the doc's own framing) it stands; read as "duplicated elsewhere"
  it does not. Tree fact stated; judgment left to the reader — not scored as a tango.
- **Minor miscount.** DEVELOPER "Kill Switches" says the diff "removes 6 lines including the
  MutexAutoLock scope"; the hunk actually deletes 8 lines (2 comment + `{` + `MutexAutoLock` +
  2× `.Clear()` + `}` + 1 blank). Substance (MutexAutoLock scope removed) is TRUE.
- **AUDIT SECTION A "Files Scanned: see payload"** is a placeholder, and the readiness score
  "🟢 95%" is a generation-1 self-assessment (2026-07-16), not a re-validation. Historical
  record only; this POR supersedes it as the room's status.

## What this POR does NOT claim

Values and symbols were verified to EXIST and to match the docs, and the patch was proven to
reproduce the live tree byte-for-byte from vanilla. They were **not** re-validated against
Mozilla documentation for correctness/optimality (the human 2-month-style doc pass), and the
upstream-vs-Gorilla authorship of the three changes was **not** resolved (no network access to
mozilla-central this pass). Contamination-screened; values not doc-validated — per the SOP's
honest-label rule.

**Room status: CLEARED (1 documentation tango flagged, 3 housekeeping flags, 0 code defects, patch byte-exact).**
