# POR — 02.GPU room clearing (2026-08-03)

Per `patches/SOP.room-clearing-and-poison-audit.md`. Method: every claim verified against
the LIVE tree (`/home/gorilla/firefox-main`) and the vanilla vault
(`…/Firefox.Scripts.Vault.Docs/SafetyVault.Firefox/firefox-main`), never against the docs.
Vanilla file copied, patch applied with `patch -p1`, result `cmp`'d against LIVE.

Folder contents: 5 `.patch` files + 1 consolidated master log
(`MASTER_PROJECT_LOG_FIREFOX_154_GPU_PATCHES.md`, 1486 lines; Part 1 dated 2026-07-05,
merged AUDIT/DEVELOPER/LAYMAN dated 2026-07-16, a verification + owner-ruling correction
dated 2026-08-02). No separate stale audit-report file — it was folded into the log.

## Friendlies (kept — verified real)

| Claim | Ground truth | Evidence |
|---|---|---|
| 5 surviving `.patch` files are true records | **vanilla + patch == live, byte-exact, clean apply (no fuzz/offset)**, all 5 | staged vault copy → `patch -p1` → `cmp`: gfxConfigManager, gfxPlatform, GfxDriverInfo, GfxInfoBase, gtk/GfxInfo |
| Wayland native compositor force-enabled with the **correct** call (golden rule 2) | present — `UserForceEnable` (NOT `UserEnable`) | gfxConfigManager.cpp:161–162, msg "Gorilla: native Wayland compositor for VA-API zero-copy overlay", inside `#ifdef MOZ_WAYLAND`, unconditional |
| Sticky sanity-test kill-switch dead-coded on **both** decode and encode | present, `false &&` ×2 | gfxPlatform.cpp:3062 (decode / featureDec) and :3114 (encode / featureEnc) |
| GfxInfoBase vendor short-circuit, **case-insensitive** | present at claimed line, `LowerCaseEqualsLiteral` for 0x8086/0x1002/0x10de → `FEATURE_STATUS_OK` | GfxInfoBase.cpp:1127–1130 (+ `@gorilla-unleashed-154` header) |
| gtk/GfxInfo vendor short-circuit answers codecs **honestly** (self-safe) | present — H264 dec/enc → OK; VP8/VP9/AV1/HEVC dec/enc → `FEATURE_BLOCKED_PLATFORM_TEST` + `FEATURE_FAILURE_GORILLA_NO_HW_CODEC`; default → OK | gtk/GfxInfo.cpp:1477–1500 (failure id :1494) |
| GfxDriverInfo un-blocklist is now **total** for Sandy/Ivy Bridge (2026-08-02 owner ruling) | 0x0166 (this machine), 0x0152, 0x0162, 0x016A, **0x015A**, full Sandy set incl. **0x010a + 0x0112** all commented out | GfxDriverInfo.cpp:207,210–213,220–226,267–274 |
| gen7 Ivy Bridge never in `IntelWebRenderBlocked` (no edit needed) | confirmed — family lists only powervr/sgx + gen4/gen4.5/gen5 | GfxDriverInfo.cpp:620–660; no 0x0152/0x0162/0x0166/0x016A/0x015A in range |
| Two-layer contract with 01.MEDIA | a design invariant, correctly stated; codec HW features stay BLOCKED so H.264-only holds | gtk/GfxInfo.cpp:1487–1495 |

## Tangoes (stale claims — all confined to the 2026-07-05 Part 1 narrative)

Unlike 03.NETWORKING (where a tango was uncorrected), **every tango here is superseded by a
later dated section inside the same consolidated file** — the merged AUDIT/DEVELOPER/LAYMAN
(2026-07-16) and the 2026-08-02 verification+correction already describe the current tree
correctly. So these are *internal staleness*, not live poison. They still fail against the
tree if read as current, so they are logged as tangoes; the action owed is a reconciliation
banner on Part 1, not a code change. Listed worst-first by how badly they could mislead a
future maintainer:

1. **"gfxPlatform.cpp — ONE SURGICAL EDIT" / encode-side "Residual (Known, Low Risk)".**
   Part 1 (log lines ~295, ~305, ~326–338, Invariant #4, Troubleshooting "Webcam Encoding")
   claims only the *decode* branch is dead-coded and the *encode* branch "still honors the
   same pref," so a stuck `media.hardware-video-decoding.failed` could kill QuickSync encode.
   **Ground truth: FALSE — the encode branch is ALSO dead-coded** (`false &&` at
   gfxPlatform.cpp:3114). Already contradicted by Part 4 (log line 1022, "encode … Line 3117
   false && ✅") and the 2026-08-02 section. **The Invariant-#4 / webcam-troubleshooting
   advice in Part 1 is obsolete.**

2. **"GTK override consciously over-reports codec support; NEVER deploy 02.GPU without
   01.MEDIA."** Part 1 (log lines 34, 258–262, 291, 499–503, Invariant #1) says the gtk layer
   reports *fake* VP9/AV1/HEVC HW support and is safe **only because** 01.MEDIA blocks those
   codecs. **Ground truth: the patch does the OPPOSITE — it blocks VP8/VP9/AV1/HEVC honestly
   at gtk/GfxInfo.cpp:1487–1495**, so 02.GPU is self-safe on the codec axis. The
   "never without 01.MEDIA" rule is now belt-and-suspenders, not a hard safety gate.
   Corrected by the merged docs and the 2026-08-02 layering note (log lines 1458–1464).

3. **"Left active: 0x015A" + Sandy set omits 0x010a/0x0112.** Part 1 (log lines ~133, 138–143)
   describes a *partial* unlock. **Ground truth: total** — 0x015A, 0x010a, 0x0112 are all now
   commented out (GfxDriverInfo.cpp:207,226,271). Superseded by the 2026-08-02 CORRECTION
   (log lines 1477–1486, owner ruling "those were supposed to be freed").

4. **"Latent Nit (Accepted): `EqualsLiteral(\"0x10DE\")` is case-sensitive → NVIDIA arm may
   never match."** Part 1 (log lines ~197–203). **Ground truth: FALSE — the code uses
   `LowerCaseEqualsLiteral(\"0x10de\")`** (GfxInfoBase.cpp:1129; same in gtk/GfxInfo.cpp:1479),
   i.e. case-insensitive; the nit does not exist.

5. **"Cosmetic Anomaly (Harmless): line ~425, NvidiaBlockD3D9Layers, GeForce 6200SE shows
   double-comment `//       //`."** Part 1 (log lines ~145–146). **Ground truth: no such
   double-comment** — GfxDriverInfo.cpp:430 is a clean, active `APPEND_DEVICE(0x0162)`; the
   current patch does not touch `NvidiaBlockD3D9Layers` at all. Describes a defect that isn't
   in the current tree. Not corrected elsewhere in the file, but harmless.

## Housekeeping flags (not poison)

- **Part 1 (2026-07-05) needs a "SUPERSEDED — see 2026-08-02 sections" banner.** It is the
  source of all five tangoes above. The file is internally correct only if read to the end;
  a maintainer who stops at the Component Documentation gets four wrong safety facts. A dated
  append-only reconciliation note is owed (I did not write it — hard constraint: single-file
  output only).
- **"Four-layer" wording vs five files.** Part 1's architecture diagram and the DEVELOPER
  doc's Module Summary still say "four-layer override," but the folder ships **five** patches
  (gfxConfigManager is the 5th / "Layer 5"). The LAYMAN doc and 2026-08-02 section correctly
  say five. Cosmetic wording drift.
- **Standing P2/P3 to-dos remain open** (per the merged AUDIT): no gtest asserting the
  vendor short-circuit keeps VP9/HEVC BLOCKED; no preflight grep guarding the commented
  `APPEND_DEVICE` lines against an upstream re-sync. These are the real regression guards for
  this room; still owed.

## What this POR does NOT claim

- **Tree only — not the binary.** All findings are source-level. The log's 2026-08-02 Level-2
  claims about the shipped `libxul.so` (e.g. "`FEATURE_FAILURE_SANITY_TEST_FAILED` ×0 — -O3
  eliminated the dead branch and its string literal") were **not re-verified** here; verifying
  them needs the built object, which is outside this read-only tree pass. Plausible from the
  `false &&` static-dead source, but labelled unverified.
- **Existence/reproduction, not optimality.** The vendor IDs (0x8086/0x1002/0x10de) and PCI
  IDs (0x0166 etc.) are hardware facts and were confirmed present as documented; they were
  **not re-validated against Mozilla blocklist documentation** for correctness/currency.
  No arbitrary tuning *values* exist in this room (blocklist unlock, not kernel co-tuned
  parameters), so no `Debian.Kernel.Work` cross-reference was required.
- Contamination-screened; values not doc-validated — per the SOP's honest-label rule.

**Room status: CLEARED — 5/5 patches real & byte-exact, all current-state claims verified;
5 stale Part-1 claims logged as tangoes but each already superseded inside the file (0 live
poison). Owed: a reconciliation banner on the 2026-07-05 narrative.**
