# POR — 04.PERFORMANCE room clearing (2026-08-03)

Per `patches/SOP.room-clearing-and-poison-audit.md`. Method: every claim verified against
the LIVE tree (`/home/gorilla/firefox-main`) and the vanilla vault
(`.../Firefox.Scripts.Vault.Docs/SafetyVault.Firefox/firefox-main`), never against the docs.
Patch reproduction = copy vanilla target → `patch -p1` → `cmp` result against live.

Folder inventory: 4 `.patch` files, 1 master log (with 5 merged sub-docs), 1 standalone
audit report. No fortress/XML dir under this topic.

## Friendlies (kept — verified real)

| Claim | Ground truth | Evidence |
|---|---|---|
| All 4 `.patch` files are true records | **vanilla + patch == live, byte-exact**, all 4 | staged vault copy, `patch -p1` (clean, no fuzz), `cmp -s` IDENTICAL for CCGCScheduler.cpp, Stencil.cpp, Maybe.h, MaybeStorageBase.h |
| CC slice budget pinned 3→4 ms | present | `dom/base/CCGCScheduler.cpp:31-32` `kICCSliceBudget = FromMilliseconds(4)` |
| CC inter-slice delay 250→120 ms | present | CCGCScheduler.cpp:29-30 `kICCIntersliceDelay = FromMilliseconds(120)` |
| Skippable delay 250→120 ms | present | CCGCScheduler.cpp:22-23 |
| Idle ICC budget 2→4 ms | present | CCGCScheduler.cpp:33-34 |
| Max ICC duration 2→1.5 s | present | CCGCScheduler.cpp:35 |
| GORILLA IVB provenance markers | present (2) | CCGCScheduler.cpp:21, :28 |
| Stencil.cpp JIT compile-cache Glean gated under `#ifndef GLEAN_DISABLED` | present | define block Stencil.cpp:9-11; include-gate :15; metric gates :3115-3118 and :3148-3150 (`hits.AddToNumerator` / `total.Add`) |
| `IsComplete<T>` SFINAE guards in both mfbt files | present | `mfbt/MaybeStorageBase.h:17,20` (trait) + :23-24 (helper); `mfbt/Maybe.h:105-106, 114-115` (helpers) used at :125-126 |
| Master-log Part 2 claim "CC 4 ms budget (line 32)" | **exact** | CCGCScheduler.cpp:32 is literally `FromMilliseconds(4)` |
| Merged-AUDIT verification commands reproduce | all 3 pass | `grep IsComplete<T>` → 1 (MaybeStorageBase) + 2 (Maybe.h); `grep kICCSliceBudget/kICCIntersliceDelay` → 4/120; `grep GLEAN_DISABLED Stencil.cpp` → present |
| Folder-hygiene (5 no-edit vanilla files dropped) | consistent | folder now holds only the 4 edited patches; deletion claim consistent with inventory (deletion not independently re-verified) |

## Tango neutralised (1)

**`AUDIT_REPORT_04.PERFORMANCE.md` (2026-07-10, "Status: PASS / 100%") is a wrong-content,
misfiled audit — its body audits NETWORKING, not the files it names.** Its Section A "Files
Scanned" correctly lists the four performance files (CCGCScheduler.cpp, Maybe.h,
MaybeStorageBase.h, Stencil.cpp), but Sections B–F describe an entirely different subsystem:

- "optimizes the Firefox network layer (Necko)"; "Sony VAIO SVE14A3AJ" socket tuning
- "DNS HostResolver thread concurrency limits raised to 16"
- "Aggressive TCP keepalives forced (15s idle, 5s probe)"
- "HTTP/3 UDP buffer settings … 64MB"; "SO_SNDBUF … left unconfigured"
- Section F proposes `SetSendBufferSize(33554432)` in `HttpConnectionUDP.cpp` and
  `kGorillaUploadChunkSize` in `nsHttpTransaction.cpp`

**Ground truth:** the four named files contain ZERO networking code — they are the CC/GC
scheduler (`CCGCScheduler.cpp`), two C++ template headers (`Maybe.h`, `MaybeStorageBase.h`),
and the JS frontend stencil (`Stencil.cpp`). Verified by full read of all four live files
plus the patch-reproduction above. This is gen-1 Gemini poison: a PASS/100% audit whose body
belongs to 03.NETWORKING pasted under a performance header. Its Section F "To Do" is also
**stale** — the UDP send-buffer it proposes was already closed there (03.NETWORKING POR,
HIGH-001, 4 MB at `HttpConnectionUDP.cpp:311`).
**Superseded by** the CORRECT merged audit inside the master log
(`04-performance.AUDIT.md`, 2026-07-16, PASS/94%), which describes the real build-compat +
CC-tuning + Stencil-gate content. **Action owed:** treat the standalone
`AUDIT_REPORT_04.PERFORMANCE.md` as void/historical; the merged 07-16 audit is authoritative.
**Do NOT** act on its networking "To Do" here.

## Housekeeping flags (precision drift, not poison — substantive claims are TRUE)

- **Stencil line numbers stale.** Master-log Part 2 says the Glean gates sit at
  "lines 3113-3116, 3144-3146"; live gates are at **3115-3118** and **3148-3150**. Off by
  ~2-4 lines; the gates exist and behave exactly as described.
- **MaybeStorageBase line span off-by-one.** Part 2 says `IsComplete` on "lines 16-20"; live
  trait is at **17-20**. Substantively correct.
- **"4 constexpr sites" is an over-count.** Merged AUDIT (07-16) says the SFINAE guards are
  "4 constexpr sites"; there are **3** `if constexpr (IsComplete<T>::value)` sites
  (MaybeStorageBase.h:24; Maybe.h:106, :115), one per helper. Guards are present; count drifts.
- **Docs under-report the full patch scope.** The DEVELOPER "Performance Profile" table lists
  only the 4 ms budget + 120 ms delay, but the CCGCScheduler patch also changes three more
  constants that ARE live: `kCCSkippableDelay` 250→120, `kIdleICCSliceBudget` 2→4,
  `kMaxICCDuration` 2 s→1.5 s. Real changes, just undocumented in the perf table.

## What this POR does NOT claim

- Patches verified to **reproduce the live tree byte-exact**, and every documented value/marker
  verified to **exist** at the stated location. Values were **not** re-validated against Mozilla
  documentation for correctness/optimality (the human 2-month doc pass). Contamination-screened;
  values not doc-validated — per the SOP's honest-label rule.
- **Build not run.** `./mach build` on Clang-21 was not executed here; the "does not compile
  without the `IsComplete` guards / ERR-BUILD-008" rationale is the documented author claim,
  not independently reproduced this session.
- **CC cadence not benchmarked.** The 4 ms/120 ms values carry an in-doc Wayland-60 Hz
  frame-budget rationale (16.6 ms) and the docs themselves label them "not benchmarked"; not
  benchmarked here either. These are CC-scheduler constants with a self-contained rationale —
  **no kernel cross-ref was required** (they are not kernel-coupled tuning).

**Room status: CLEARED (1 tango neutralised — misfiled networking audit; 0 open code items; all 4 patches byte-exact against live).**
