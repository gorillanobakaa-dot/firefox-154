# POR (DRAFT) — 13.TELEMETRY.KILL room clearing (2026-08-03)

Per `patches/SOP.room-clearing-and-poison-audit.md`. Method: every claim verified against
the LIVE tree (`/home/gorilla/firefox-main`) and the vanilla vault
(`/home/gorilla/.../SafetyVault.Firefox/firefox-main`), never against the docs.
Verification = stage vanilla file → `patch -p1` → `cmp` vs live, plus sha256 cross-checks
and live-tree greps.

**Doctrine note (kept in mind throughout):** this room implements telemetry-off via
STUBS / const-guards / compile-time DCE, NOT excision. The project deliberately abandoned
excision (NS_ERROR_FACTORY_NOT_REGISTERED + 157 shim headers). The stub IS the intended
design. Nothing below flags the stub approach as "incomplete" or recommends re-excision.
Every guard the docs *claim* is present was checked and IS present in the live tree.

## Friendlies (kept — verified real)

| Claim | Ground truth | Evidence |
|---|---|---|
| 6 code `.patch` files are true records | **vanilla + patch == live, byte-exact**, all 6 | staged vault copy, `patch -p1` rc=0, `cmp -s` MATCH — FOG.cpp, MemoryTelemetry.cpp, dispatcher/global.rs, lib.rs, memory_distribution.rs, timing_distribution.rs |
| `GORILLA_TELEMETRY_OFF: bool = true` const | present | `third_party/rust/glean-core/src/lib.rs:115` |
| FOG::InitializeFOG no-ops before `fog_init()` | present | `toolkit/components/glean/xpcom/FOG.cpp` — Gorilla comment :150, `return NS_OK;` :153 (before RunOnShutdown/fog_init) |
| MemoryTelemetry::GatherReports early-return (skips /proc/self/smaps) | present | `xpcom/base/MemoryTelemetry.cpp` — Gorilla comment :231, `return NS_OK;` :239 (before nsMemoryReporterManager path) |
| dispatcher::launch drops task when not test-mode | present | `third_party/rust/glean-core/src/dispatcher/global.rs:54` `if !TESTING_MODE...` → `:55 drop(task); return;` |
| 4 documented rust files' live sha256 == checksum-patch `+` hashes | exact match, all 4 | python sha256 vs live `.cargo-checksum.json` (global.rs `f4247e…`, lib.rs `d3883e…`, memory_dist `43c316…`, timing_dist `e18999…`) |
| Live `.cargo-checksum.json` internally consistent | **NO stale entries** — every one of the 19 edited files' recorded hash equals its actual live content | full-tree sha256 sweep, 0 mismatches (so the shipped build's checksum guard is satisfied) |
| Stub/soft-disable doctrine (symbols/factories preserved) | confirmed — all edits are early-returns / const guards; no symbol removed | diffs are pure insertions, `package` hash unchanged `bad39d9…` |

## Tangoes (with proof)

These are **not** the dangerous overclaim class (a claimed-active guard that is actually
absent). They are the inverse: **the patch set + docs are an INCOMPLETE record of a live
tree that does MORE than they describe.** Still worth neutralising — they break
reproducibility and mislabel the residual.

### T1 — `.cargo-checksum.json.patch` does NOT reproduce the live checksum; 15 real edits have NO `.patch` file

- **Proof:** staged vanilla `.cargo-checksum.json` + patch, `cmp` vs live → **differ at byte 2496**.
  JSON semantic compare: **15 `src/metrics/*.rs` hash entries differ** from live —
  `boolean, counter, custom_distribution, datetime, denominator, event, numerator, object,
  quantity, rate, string, string_list, text, timespan, uuid`. The patch's `+` side left all
  15 at their **vanilla** hashes; the live checksum carries their **edited** hashes.
- **Root cause:** the live tree contains a **second wave** of GORILLA_TELEMETRY_OFF guards —
  **25 guard lines across those 15 metric files** (full-tree sweep: 19 modified glean-core
  files total = the 4 documented + 15 undocumented). All 15 are the terse single-line style
  `if crate::GORILLA_TELEMETRY_OFF { return; }  // Gorilla: telemetry disabled — skip clone + dispatch`,
  distinct from the documented files' multi-line comment blocks — consistent with edits made
  **after** the 2026-07-16/17 docs+patches were generated and never back-documented.
- **Consequence:** `ls` confirms **zero** `.patch` files for any of the 15. Rebuilding the
  telemetry kill from *only this folder* would apply 6 code patches + a checksum patch that
  are mutually consistent, compile cleanly, and produce a **weaker** kill than what ships
  (4 guarded files, not 19). The shipped binary is fine; the *record* is not reproducible.
- **How it would be detected / falsifiable:** `diff <(python -c 'sha256 of live metric files')`
  against the checksum patch `+` side; or `grep -rl GORILLA_TELEMETRY_OFF live/.../src/metrics`
  → **18 files** vs the 4 the patch set names.
- **Action owed (not taken — read-only pass):** generate the 15 missing `.patch` files and a
  corrected `.cargo-checksum.json.patch`, OR document that this folder is a partial record and
  point to the live tree as authority. **Do NOT "fix" by reverting the extra guards** — they
  are correct, on-doctrine, and already shipped.

### T2 — Stale doc claim: "custom_distribution is unguarded" — it is NOT

- **Proof:** live `third_party/rust/glean-core/src/metrics/custom_distribution.rs` has **two**
  guards at **:109 and :129** (diff vs vanilla = 2 inserted guard lines).
- **What the docs say (all stale):**
  - AUDIT track (master log): "unguarded custom_distribution" listed as accepted residual;
    "Phase 2 — custom_distribution.rs … currently unguarded" as a *future* tweak.
  - DEVELOPER track: "custom_distribution is unguarded (~0.10%)".
  - The residual-`0.39%` accounting attributes ~0.10% to this "unguarded" path.
- **Reality:** Phase 2 is **already done**. The "~0.10% unguarded custom_distribution" line of
  the residual budget is stale and over-states the residual by that amount.
- **Action owed (not taken):** correct the master log's residual accounting and mark Phase 2
  DONE. Append-only; do not edit existing merged text in place per project rules.

## Housekeeping (not poison)

- `Cargo.toml.orig` entry: present in vanilla checksum, **absent** in both the patch `+` side
  and live. Consistent (a benign vendoring-artifact drop) — no action.
- `PRECHECK.json` = `[]` and `PRECHECK.md` = 0 findings: consistent with clean offline rules.
- Master log is a 2026-08-02 consolidation of AUDIT/DEVELOPER/LAYMAN/PRECHECK verbatim; its
  file-inventory SHA256s are of the *patch files* (historical bookkeeping), not tree state —
  not a tree claim, no action.
- The doc-stated perf figures carry explicit dates/method ("VERIFIED via perf 2026-07-16");
  they are labelled measurements, treated as such below.

## What this POR does NOT claim

- **Perf numbers not re-measured.** 13.2%→0.39%, 8.9%→0.02%, 3.5%→0.00%, 0.84%→0.39% are
  runtime profiler results requiring a build + `perf` on target hardware. This pass verified
  the *mechanisms* (guards/early-returns/checksum) EXIST in the live tree as claimed; it did
  **not** reproduce the measurements. Not verified here.
- **Not a build test.** "vanilla+patch == live byte-exact" proves the patches faithfully
  record the edits; it does **not** prove the crate actually recompiled on the shipped build
  (the doc's own MEDIUM debt: a stale `libglean_core-*.rlib` can silently reuse old code).
  Not checked (no build performed).
- **Contamination-screened; values not doc-validated.** Guard *placement* was not re-validated
  against Mozilla/Glean upstream for hot-path optimality — the human doc pass, not done here.
- **Scope = this room's declared files.** No sweep for telemetry edits outside glean-core /
  FOG / MemoryTelemetry.

## Room status

**CLEARED WITH FINDINGS — 0 dangerous overclaims (every claimed guard verified present in the
live tree; stub doctrine intact and correct), 2 record-integrity tangoes owed:**
T1 = 15 shipped metric-guard edits have no `.patch` and the checksum patch omits them (patch
set is a partial, non-reproducible record — live tree does MORE than documented);
T2 = stale "custom_distribution unguarded" claim (Phase 2 already done). Both actions deferred
(read-only pass); neither warrants reverting any live guard.
